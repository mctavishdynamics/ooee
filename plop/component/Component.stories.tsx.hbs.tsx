import type { Meta } from "@storybook/react";

import { NAME____ } from "./NAME____.tsx";

const meta = {
  title: "NAME____",
  component: NAME____,
  tags: ["autodocs"],
  parameters: {
    layout: "centered",
  },
  args: {},
} satisfies Meta<typeof NAME____>;

export default meta;

export const Default = () => {
  return <NAME____ />;
};
